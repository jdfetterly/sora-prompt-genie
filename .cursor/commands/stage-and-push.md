# stage-and-push

git add . && git commit -m "Update changes" && git push

This command will be available in chat with /stage-and-push

Stages all changes, commits with a default message, and pushes to the current branch.

