# restart-dev

npm run dev

This command will be available in chat with /restart-dev

Starts the development server. If a dev server is already running, stop it first (Ctrl+C) before using this command.

