# OpenRouter API Key - Required for AI functionality
OPENROUTER_API_KEY=your_api_key_here

# Server Port - Optional, defaults to 5000
PORT=5173

# Node Environment - Set to "development" for local dev
NODE_ENV=development

# Note: DATABASE_URL is not needed for initial local setup.
# Firebase will be integrated later for user authentication and prompt saving features.

# OpenRouter Model - Optional, defaults to claude-3-haiku (cheaper)
# Options:
#   - anthropic/claude-3-haiku (cheapest Claude, ~$0.25/$1M tokens)
#   - google/gemini-flash-1.5 (very cheap, ~$0.075/$1M tokens)
#   - openai/gpt-3.5-turbo (cheap, ~$0.5/$1M tokens)
#   - anthropic/claude-3.5-sonnet (expensive, ~$3/$1M tokens)
OPENROUTER_MODEL=anthropic/claude-3-haiku
